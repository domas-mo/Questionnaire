import styled from 'styled-components';

const StyledMessage = styled.div`
    text-align: center;
    color: #185bf1;
    padding: 2rem;
    margin: 20px auto;
`;

export default StyledMessage;